import { createElement } from '../../utils/elementCreator'
import './salesPage.scss'
import { SalesPageParam } from './types'

export function showSalesPage(root:HTMLElement){
const salesPage = createElement(SalesPageParam, root)
}