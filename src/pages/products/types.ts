export enum ProductsPageParam {
  elemTag = 'div',
  classNames = 'page__products',
}
export enum ProductsList {
  elemTag = 'div',
  classNames = 'products__list',
}

export enum ProductCard {
  elemTag = 'div',
  classNames = 'products__card',

}
export enum ProductName {
  elemTag = 'p',
  classNames = 'product__name',
  innerText='product'

}