export enum SliderContainer {
  elemTag = 'div',
  classNames = 'slider__container',
}
export enum LeftBtn {
  elemTag = 'div',
  classNames = 'slider-btn',
  innerText = '<',
  id = 'prev',
}
export enum RightBtn {
  elemTag = 'div',
  classNames = 'slider-btn',
  innerText = '>',
  id = 'next',
}
export enum Slider {
  elemTag = 'div',
  classNames = 'slider',
}
export enum SlideParamLink {
  elemTag = 'a',
  classNames = 'slider__link',
}
export enum SlideParamImage {
  elemTag = 'img',
  classNames = 'slider__img',
}
