export enum SortBlockParam {
  elemTag = 'div',
  classNames = 'products__panel',
}

export enum SearchContentParam {
  elemTag = 'div',
  classNames = 'panel__search',
}
export enum SortContentParam {
  elemTag = 'div',
  classNames = 'panel__sort',
}

export enum SearchFieldInput {
  elemTag = 'input',
  type = 'text',
  classNames = 'search__input',
  id = 'search-field',
  required = '',
}

export enum SearchFieldLabel {
  elemTag = 'label',
  for = 'search-field',
  classNames = 'search__label',
  innerText = '',
}
export enum SearchFieldButton {
  elemTag = 'div',
  classNames = 'search__button',
}

export enum SortSelect {
  elemTag = 'select',
  classNames = 'sort__field',
}
