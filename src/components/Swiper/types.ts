export enum SwiperContainer {
  elemTag = 'div',
  classNames = 'swiper',
}
export enum SwiperWrapper {
  elemTag = 'div',
  classNames = 'swiper-wrapper',
}
export enum SwiperSlide {
  elemTag = 'div',
  classNames = 'swiper-slide',
}
export enum SwiperSlideContainer {
  elemTag = 'div',
  classNames = 'swiper-slide-container',
}
export enum SwiperImage {
  elemTag = 'img',
  classNames = 'swiper-slide-image',
  alt = 'slider-image',
}
export enum SwiperPagination {
  elemTag = 'div',
  classNames = 'swiper-pagination',
}

export enum SwiperBtnPrev {
  elemTag = 'div',
  classNames = 'swiper-button-prev',
}
export enum SwiperBtnNext {
  elemTag = 'div',
  classNames = 'swiper-button-next',
}

export enum SwiperScroll {
  elemTag = 'div',
  classNames = 'swiper-scrollbar',
}
