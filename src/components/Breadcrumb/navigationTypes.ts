export enum BreadcrumbBlockParam {
  elemTag = 'div',
  classNames = 'breadcrumb__container',
}
export enum BreadcrumbListParam {
  elemTag = 'div',
  classNames = 'breadcrumb__list',
}
export enum BreadcrumLinkParam {
  elemTag = 'a',
  classNames = 'breadcrumb__link',
}
